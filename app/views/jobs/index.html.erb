<%= render 'layouts/page_layout' %>

  <section class="positions-first col-md-12">
    <div class="openings">
      <h1 class="category-header">Openings</h1><p class="openings-count"><%= @openings.count %> openings</p>
      <%= link_to ' + New Opening', new_company_job_path, :class => "new-opening-button" %>
        <h4 class="openings-header col-md-6">From here, you can create new job openings by clicking the button on the right, or view/edit current openings below. You can also jump directly to an openings' applicants to view their profiles.</h4>
      <h2 class="current-header">Current</h2>
        <% if @current_positions.empty? %>
          <p><i>Doesn't look there are any openings right now. You can add an opening by clicking 'New Opening' at the top of this page</i></p>
        <% else %>
          <div class="open-positions-list">
            <ul>
              <% @current_positions.order("created_at DESC").each do |job| %>
                <li class="position">
                  <h2 class="job-title">
                    <%= link_to company_job_url(:id => "#{job.id}") do %>
                    <%= job.title %>
                    <% end %>
                  </h2>
                  <p class="job-code">(ID: <%= job.job_code %>)</p>
                  <p class="time-ago"><i>Created <%= time_ago_in_words(job.created_at) %> ago</i></p><br>

                  <% if job.live_status == true %>
                    <p class="live-status-true col-md-offset-2">Currently active</p>
                  <% elsif job.live_status == false %>
                    <p class="live-status-false">Currently inactive</p>
                  <% end %>
      
                  <% @applicants.where(:job_id => job.id ).order("created_at DESC").each do |application| %>
                    <ul>
                      <li class="job-applicant col-md-2"><%= link_to "#{application.applicant.full_name}", applicant_path(:id => application.applicant.id) %></li> <p class="application-status"><%= application.application_status %></p>
                    </ul>
                  <% end %>
            
                    <!--<%#= link_to job_applicants_path(:job_id => "#{x.id}") do %>
                    <%#= JobApplication.where(:job_id => "#{x.id}").count %>
                    applicants -->
                    <!-- <%# end %> have applied for this position -->

                </li>
              <% end %>
            </ul>
          </div>
        <% end %>
    </div>
  </section>

<%= render 'layouts/page_layout_closing_tags' %>
